//! null

/*
- The Null type has exactly one value, called null
- It cannot be boxed (?)
- It means 'no object' and is encounter in JS almost always if user introduced it
  i.e. JS almost never returns null value' (but rather 'undefined').
*/
